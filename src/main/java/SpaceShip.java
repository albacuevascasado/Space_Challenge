public interface SpaceShip {
/*
interface methods do not have body
 */
     boolean launch_Explosion(int actual_Capacity);
     boolean land_Explosion(int actual_Capacity);
     boolean canCarry(int weight);
     int carry(Item item);

}
